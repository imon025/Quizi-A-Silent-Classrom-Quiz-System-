name: quizi
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # Firebase
  firebase_core: ^4.2.1
  firebase_auth: ^6.1.2
  cloud_firestore: ^6.1.1
  firebase_storage: ^13.0.5

  # UI & state management
  provider: ^6.1.5
  shared_preferences: ^2.2.3
  fluttertoast: ^8.2.2
  intl: ^0.18.1

  # UI/UX packages
  google_fonts: ^6.0.0
  percent_indicator: ^4.2.5

  # Optional utilities
  path_provider: ^2.1.5
  http: ^1.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # Assets removed temporarily
  # assets:
  #   - assets/images/
  #   - assets/icons/

  # Fonts removed temporarily
  # fonts:
  #   - family: Poppins
  #     fonts:
  #       - asset: assets/fonts/Poppins-Regular.ttf
  #       - asset: assets/fonts/Poppins-Bold.ttf
  #         weight: 700
